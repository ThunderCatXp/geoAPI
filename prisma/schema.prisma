generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model location {
  id        Int    @id @default(autoincrement())
  country   String
  city      String
  latitude  Float
  longitude Float
  altitude  Float
}
