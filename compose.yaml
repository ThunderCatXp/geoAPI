services:
  web:
    build: ./web
    ports:
      - "8080:8080"
    networks:
      - app
  api:
    build: ./api
    ports:
      - "3000:3000"
    networks:
      - app
networks:
  app:
    driver: bridge
